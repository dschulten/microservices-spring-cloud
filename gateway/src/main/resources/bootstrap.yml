spring:
  application:
    name: gateway
  cloud:
    consul:
      host: consul
      # config:
        # format: YAML
      # discovery:
        # register-health-check: false
# 
    # loadbalancer:
      # ribbon:
        # enabled: false
# 
# server.port: 8080